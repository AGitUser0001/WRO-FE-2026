# Jetson install ROS2

## Ref document link here:
https://docs.ros.org/en/humble/Installation/Ubuntu-Install-Debs.html

## Set locale
```
locale
apt update && apt install locales
locale-gen en_US en_US.UTF-8
update-locale LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8
export LANG=en_US.UTF-8

locale
```

## Setup Sources
```
apt install software-properties-common
add-apt-repository universe

apt update && apt install curl -y
export ROS_APT_SOURCE_VERSION=$(curl -s https://api.github.com/repos/ros-infrastructure/ros-apt-source/releases/latest | grep -F "tag_name" | awk -F\" '{print $4}')
curl -L -o /tmp/ros2-apt-source.deb "https://github.com/ros-infrastructure/ros-apt-source/releases/download/${ROS_APT_SOURCE_VERSION}/ros2-apt-source_${ROS_APT_SOURCE_VERSION}.$(. /etc/os-release && echo ${UBUNTU_CODENAME:-${VERSION_CODENAME}})_all.deb"
dpkg -i /tmp/ros2-apt-source.deb
```

## Install ROS 2 packages
```
apt update
apt upgrade

apt install ros-humble-desktop
```

## Environment setup
**Before running any ROS 2 commands, make sure to run the source command first.**
```
source /opt/ros/humble/setup.bash
```

## Run a ROS 2 Test
### Start the listener first (to listen for messages)
```
ros2 run demo_nodes_py listener
```

### Then start the talker (to publish messages)
```
ros2 run demo_nodes_cpp talker
```

### Go back to the listener window and verify that it continues to receive messages

